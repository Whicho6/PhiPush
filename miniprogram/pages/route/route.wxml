<view class="page"><view class="section">目标 {{route.target_rks}}</view><view class="card"><view class="label">ESTIMATED FINAL</view><view class="rks">{{route.estimated_final_rks}}</view><view class="muted">{{route.current_rks}} → {{route.target_rks}}</view></view><view wx:for="{{route.steps}}" wx:key="index" class="card"><view class="row"><b>{{index+1}}. {{item.song}}</b><text class="pill">{{item.difficulty}}</text></view><view class="row"><text>{{item.from_accuracy}}%</text><text>→</text><text class="acid">{{item.to_accuracy}}%</text></view><view class="gain">总 RKS +{{item.estimated_total_rks_gain}}</view></view><view class="muted small">{{route.disclaimer}}</view></view>
